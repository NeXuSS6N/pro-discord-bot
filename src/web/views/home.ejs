<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Pro Discord Bot</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
    <style>
      body { font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, sans-serif; margin:0; background: #0f172a; color: #e2e8f0; }
      header { display:flex; justify-content:space-between; align-items:center; padding: 24px; }
      a.btn { background:#6366f1; color:white; padding:10px 16px; border-radius:10px; text-decoration:none; font-weight:600; }
      a.btn:hover { background:#4f46e5; }
      .hero { padding: 96px 24px; text-align:center; }
      .hero h1 { font-size: 48px; margin:0 0 16px; }
      .hero p { color:#94a3b8; font-size:18px; }
      footer { text-align:center; padding:24px; color:#64748b; }
    </style>
  </head>
  <body>
    <header>
      <div style="display:flex; gap:12px; align-items:center;">
        <img src="/static/logo.svg" width="28" onerror="this.style.display='none'"/>
        <strong>Pro Discord Bot</strong>
      </div>
      <div>
        <% if (user) { %>
          <a href="/dashboard" class="btn">Dashboard</a>
          <a href="/logout" class="btn" style="background:#ef4444;margin-left:8px;">Logout</a>
        <% } else { %>
          <a href="/login" class="btn">Login with Discord</a>
        <% } %>
      </div>
    </header>
    <section class="hero">
      <h1>Modern Bot. Powerful Features.</h1>
      <p>Manage your server with style. Slash commands, moderation, utilities, economy, and a sleek web dashboard.</p>
    </section>
    <footer>
      Logged in as: <%= user ? user.username : 'Guest' %> — Bot: <%= botUser ? botUser.tag : '...' %>
    </footer>
  </body>
  </html>

